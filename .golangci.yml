version: "2"

run:
  timeout: 5m
  tests: true

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - revive
    - ineffassign

linters-settings:
  revive:
    severity: warning
    rules:
      - name: exported

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
